# Setup python environnement (Windows)
$ python -m venv ./venv
$ python -m source venv/bin/activate

(Select the python source from ./venv as python interpreter in vscode)

$ python -m pip install -r requirements.txt